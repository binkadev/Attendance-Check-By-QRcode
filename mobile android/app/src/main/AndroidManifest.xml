<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AttendenceCheckQRcode"
        android:usesCleartextTraffic="true">

        <activity
            android:name=".ui.qr.QRScanActivity"
            android:exported="false" />
        <activity
            android:name=".ui.settings.SettingsActivity"
            android:exported="false" />
        <activity
            android:name=".ui.stats.StatsActivity"
            android:exported="false" />
        <activity
            android:name=".ui.clazz.CreateClassActivity"
            android:exported="false" />
        <!--        <activity-->
        <!--            android:name=".ui.attendance.AttendanceActivity"-->
        <!--            android:exported="false" />-->
        <activity
            android:name=".ui.home.HomeActivity"
            android:exported="false" />
        <!-- <activity -->
        <!-- android:name=".ui.signup.SignUpActivity" -->
        <!-- android:exported="false" /> -->
        <activity
            android:name=".ui.auth.SignUpActivity"
            android:exported="false" />
        <activity
            android:name=".ui.auth.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- CAM Qr Code -->
        <!--        <uses-permission android:name="android.permission.CAMERA" />-->

        <!--        <uses-feature-->
        <!--            android:name="android.hardware.camera"-->
        <!--            android:required="false" />-->
        <activity android:name=".ui.clazz.ClassListActivity" />
        <activity android:name=".ui.clazz.ClassDetailActivity" />

        <activity android:name=".ui.teaching.TeachingListActivity" />
        <activity android:name=".ui.teaching.TeachingDetailActivity" />
        <activity android:name=".ui.teaching.CreateQRActivity" />
    </application>

</manifest>